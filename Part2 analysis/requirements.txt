# WAF规则智能识别与分析工具 - 依赖包清单

# Web框架
flask==2.3.3
flask-cors==4.0.0

# 解析器
antlr4-python3-runtime==4.13.1

# 数据处理和分析
networkx==3.1
matplotlib==3.8.2
numpy==1.26.2
pandas==2.1.4

# 可视化
pyecharts==2.0.0  # 替换原“echarts”（Python中ECharts封装包实际为pyecharts，支持Python 3.8+）
# 原“d3==7.8.5”为JavaScript库，Python中无直接对应包，若需类似可视化能力，可通过pyecharts或plotly等库实现

# 文件处理和系统操作
python-dotenv==1.0.0
# pathlib为Python标准库，无需单独安装
# logging为Python标准库，无需单独安装

# 开发工具
pytest==7.4.3
flake8==6.1.0
black==23.10.1

# 其他
requests==2.31.0
urllib3==2.0.7