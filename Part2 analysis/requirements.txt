# WAF规则智能识别与分析工具 - 依赖包清单（已清理）

# 说明：
# - 移除了不应通过 pip 管理的前端库（如 d3/echarts），建议通过 npm/yarn 或将静态文件放到 frontend/static。
# - 移除了 Python 标准库回移植包（如 pathlib、logging）。
# - 不直接固定 urllib3 版本，以避免与 requests 的依赖冲突；让 pip 解析兼容版本。

# Web 框架
flask==2.3.3
flask-cors==6.0.0

# 解析器
antlr4-python3-runtime==4.13.1

# 数据处理与分析
networkx==3.1
matplotlib==3.8.2
numpy==1.26.2
pandas==2.1.4
pyvis==0.3.1    # 可选：用于图网络可视化（如项目中使用则保留）

# 文件/环境
python-dotenv==1.0.0

# coreruleset 子工具依赖（来自 coreruleset util）
msc-pyparser>=1.2.1

# 开发/测试工具
pytest==7.4.3
flake8==6.1.0
black==23.10.1

# 其他网络库（使用较新的 requests）
requests==2.32.4